version: '2'
services:
  nginx:
    image: nginx
    networks:
      - default
      - web
    links:
      - php
    volumes_from:
      - php
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./php_inc:/etc/nginx/php_inc:ro
    environment:
      - VIRTUAL_HOST=rss.zfix.org,rss.mrksr.de
      - CERT_NAME=shared

  php:
    image: mrksr/php
    networks:
      - default
      - ldap
    links:
      - database
    external_links:
      - ldap
    volumes:
      - /srv/tt-rss/tt-rss:/srv

  daemon:
    image: mrksr/php
    links:
      - database
    volumes:
      - /srv/tt-rss/tt-rss:/srv
    command:
      - /srv/update_daemon2.php
    user: www-data

  database:
    image: postgres
    volumes:
      - /srv/tt-rss/postgres:/var/lib/postgresql/data
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres

networks:
  web:
    external:
      name: nginx_default
  ldap:
    external:
      name: ldap_default
